import React from 'react';

export default (props) => {

    const getElementState = () => {
        return {id: 4, text: 'Test', component: 'ButtonElement'}
    };

    return (
        <div>It's Button</div>
    );
}
